# D1 第三方扩展验收实验报告（迭代 2 · 2026-08-26）

> 验收项：迭代 2 D1——「一个不读内部代码的用户从模板写新节点+新传感器；
> 记录全部摩擦点并修复；时长可复测（『X 分钟接入』成为数据）」。
> 方法：干净上下文的 AI 子代理扮演首次接触仓库的第三方开发者，
> 只许读 README/CLAUDE.md/doc(s) 文档与 CMakeLists（构建注册），
> 文档明确指向的参照实现除外——翻源码解决的问题记为摩擦。

## 一测（修复前）：19.5 分钟 / 8 摩擦点 / 零改框架

任务：模拟超声波传感器 + UltrasonicNode，从读文档到编译测试全绿。

**结果**：21:52:54 → 22:12:20（约 19.5 min，其中 4 min 为全量测试）。
一行框架代码未改（CRTP `SensorBase` 模板干净可用、注册表可编程访问），
新测试 5/5 通过、全量 0 回归。产物（6 新文件 + 2 处构建注册）转正为
官方扩展范本（`hal/sensor/ultrasonic_sensor.hpp` + `ultrasonic_node.*`）。

### 摩擦点 → 根因 → 处置

| # | 摩擦 | 根因 | 处置 |
|---|------|------|------|
| 1 | 三份指南引用不存在的路径（`domain/perception/sensor_interface.hpp` 等） | HAL 迁移（ITERATION D1 方案 B）后文档没跟 | 09/11/sensor-pipeline 路径对账 ✅ |
| 2 | 注册宏文档签名错（`REGISTER_SENSOR(lidar, velodyne, X)` / `amr::domain::sensor`） | 文档写于命名空间定形前 | 对账为 `AMR_REGISTER_SENSOR("...","...",X)` + `amr::hal::sensor` ✅ |
| 3 | 适配器文件放哪没讲 | 同 #1 的迁移残留 | guides/11 路径改为包内 `hal/sensor/` ✅ |
| 4 | CLAUDE.md「指标由基类托管」过度承诺 | MetricsRegistry 字段是封闭集合 | CLAUDE.md 改为如实表述 ⚠️ 字段封闭 ✅ |
| 5 | **`AMR_REGISTER_SENSOR` 宏本身无法编译**（token 粘贴字符串字面量） | 宏体 `##category##type_str` 对字符串非法 | **修宏**：`__LINE__` 拼名 + `__attribute__((used))`，树内实际使用（ultrasonic 注册 TU）进回归 ✅（审计 P1-b 同项） |
| 6 | **静态注册对象被链接器丢弃**：静态库中无引用的注册 TU 不进产物，`make_sensor` 返回 nullptr | 自注册模式与静态库链接的固有冲突；内置传感器靠显式 `register_builtin_sensors()` 绕开，第三方无入口 | registry.hpp 头注释确立**两条注册路径**：幂等显式注册（推荐/唯一可靠）+ 宏（TU 必然被链接时）；CLAUDE.md 同步 ✅ |
| 7 | 独立可执行节点的构建注册（`INDEPENDENT_NODES`）无文档 | 「注册表一行」只写了 compute 容器形态 | CLAUDE.md 补一行 ✅ |
| 8 | `config/sensors.yaml` 无任何代码加载，文档暗示「改 YAML 一行切换」 | 悬挂的文档形状配置 | sensors.yaml 头部加真相注记（参数速查，不随 launch 加载；实际接线是 ROS 参数）+ CLAUDE.md 同步 ✅ |

### 一测结论（原文）

- 「加传感器零改框架」：**字面属实，路径不属实**——宏编不过（#5）、编过也会被
  链接器丢（#6）；能落地的方式（幂等显式注册）文档从未告诉第三方。
- 「新节点从模板起步」：基本属实——CLAUDE.md → imu_node → AmrNode 链路
  完整可抄，是全程最顺的部分。

## 二测（修复后）：8 分 48 秒 / 2 摩擦 / 一次全过

同一规则的新「用户」（干净上下文子代理），自选任务：温度传感器 + 驱动节点
+ **消费节点**（订阅/滚动均值/阈值告警）+ 端到端测试 + 双进程冒烟——范围
大于一测。22:22:03 → 22:30:51 全部一次通过，35/35 无回归。

一测踩过的「静态库自注册被链接器丢弃」坑，三处文档（CLAUDE.md/registry.hpp
/ultrasonic 范本）修复后本次零时间浪费——**修复生效的直接实证**。

剩余 2 摩擦当场处置：① ctest 前置 source 未写 → README 补；② 消费方节点
无范本 → temperature_monitor_node 转正为范本 + CLAUDE.md 指向（本次入库）。

| 指标 | 一测（修复前） | 二测（修复后） |
|------|--------------|--------------|
| 总耗时 | 19.5 min | **8 min 48 s**（范围还翻倍：+消费节点+端到端测试+双进程冒烟） |
| 摩擦点数 | 8（含 2 阻断级） | **2**（1 环境类 + 1 轻度文档缺失，0 阻断） |
| 一次通过 | 否（宏编不过需绕行） | **是**（编译/测试/冒烟全部首次通过） |
| 零改框架 | ✅ | ✅（35/35 全量无回归） |

## 结论与后续

- D1 验收标准达成方式：摩擦点全部有处置（文档对账 6 项 + 宏修复 1 项 +
  真相注记 1 项），「X 分钟接入」成为可复测数据；
- ultrasonic 全链范本（传感器/注册两路径/节点/测试/构建注册）入库，
  guides/11 与 CLAUDE.md 指向它作为参照；
- 遗留改进项（低优）：MetricsRegistry 开放 per-sensor 通用维度（#4 的
  代码级解，当前如实文档化）；宏在共享库场景的 whole-archive 验证。
